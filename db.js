const sqlite3 = require('sqlite3').verbose(); const path = require('path'); const DB_FILE = path.join(__dirname, 'mealmind.db'); const db = new sqlite3.Database(DB_FILE); function initDb(){ db.serialize(()=>{ db.run(`CREATE TABLE IF NOT EXISTS recipes (id INTEGER PRIMARY KEY AUTOINCREMENT, source TEXT, name TEXT, time INTEGER, estimatedCost REAL, servings INTEGER, tags TEXT, goals TEXT, mealType TEXT, ingredients TEXT, image TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`); db.run(`CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY AUTOINCREMENT, email TEXT UNIQUE, password TEXT, name TEXT, allergies TEXT, is_admin INTEGER DEFAULT 0, is_premium INTEGER DEFAULT 0, created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`); }); console.log('DB initialized at', DB_FILE);} module.exports = { db, initDb };